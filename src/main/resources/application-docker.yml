spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

# No fallback here: a deployment without JWT_SECRET must fail to start, not run with a known key.
jwt:
  secret: ${JWT_SECRET}
